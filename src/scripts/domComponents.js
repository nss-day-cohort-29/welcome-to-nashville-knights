let resultsContainer = document.querySelector("#results-container");
let parkArray = []
let parkArryObject = {
    park_name:'',
    park_address:''  
}
  const parkHTML = (park_name, mapped_location_address ) => {
     let HTMLparkSection = 
      (`
      <section class="parkResult">
        <p>${parkk.park_name}</p>
        <p>${parkk.mapped_location_address}</p>
      </section>
     `)
   }

